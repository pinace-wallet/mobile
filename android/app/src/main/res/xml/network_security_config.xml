<?xml version="1.0" encoding="utf-8"?>
<!-- The Pinace indexer is served over HTTPS (Railway) since this repo no
     longer defaults to a plain-HTTP host. No cleartext exception needed. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
