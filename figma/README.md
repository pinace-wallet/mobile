# Figma reference

Drop the Figma CSS exports for the mobile screens here — they are the source of truth for visual polish (colors, spacing, gradients). The Flutter theme in lib/core/theme/ gets refined against these files.
